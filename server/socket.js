'use strict'

var sharedsession = require( 'express-socket.io-session' ),
    io = require( 'socket.io' ),
    exec = require( 'child_process' );

var socket = function ( httpServer, session ) {
    

};

module.exports = socket;
